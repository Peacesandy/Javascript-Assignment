let usersTransaction = prompt(` Welcome! Please select transaction Type
   1. Transfer
   2. Airtime (self)
   3. Airtime (others)
`)
 
if (usersTransaction == 1) {
    let options = prompt(` Select Destination Bank.
     1. UBA
     2. Fidelity
     3. Access
     4. First bank
     5. More
    `)
}  if (usersTransaction ==  1 || 2 || 3 || 4) {
      prompt(`Enter Amount`)
}

